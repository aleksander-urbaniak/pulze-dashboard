services:
  pulze-dashboard:
    image: pulze-dashboard:${APP_VERSION:-v1.0.0}
    build:
      context: .
      args:
        APP_VERSION: ${APP_VERSION:-v1.0.0}
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      APP_VERSION: ${APP_VERSION:-v1.0.0}
    volumes:
      - pulze-data:/app/data

volumes:
  pulze-data:
