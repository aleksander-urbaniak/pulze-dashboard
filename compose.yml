services:
  pulze-dashboard:
    image: pulze-dashboard:v1.0.0
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    volumes:
      - pulze-data:/app/data

volumes:
  pulze-data:
