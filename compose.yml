services:
  pulze-dashboard:
    image: aleksanderurbaniak/pulze-dashboard:latest
    ports:
      - "3000:3000"
    environment:
      APP_BASE_URL: ${APP_BASE_URL:-}
    volumes:
      - pulze-data:/app/data

volumes:
  pulze-data:
