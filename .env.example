# Public URL where users access PulZe.
# Used to build SAML callback/redirect URLs (must match your IdP setup).
APP_BASE_URL=https://pulze.example.com

# Optional: allow unsigned SAML responses if your IdP has no signing cert configured.
# Keep this false in production unless you fully trust your network/IdP flow.
SAML_ALLOW_UNSIGNED=false